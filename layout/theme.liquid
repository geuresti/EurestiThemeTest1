<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta charset="utf-8">
        <link rel="stylesheet" href="{{ 'style.css' | asset_url }}">

        {{ content_for_header }}
    </head>
    <body>
        {% section 'header' %}

        <!-- this prevents my fixed nav bar from hiding content -->
        <div style="padding-top: 150px"></div>
    
        {{ content_for_layout }}

        {% section 'footer' %}
    </body>
</html>